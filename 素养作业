1.git
可以使得线上的github与本地的仓库建立联系
是从服务器上克隆完整的Git仓库（包括代码和版本信息）到单机上的软件
是将本地仓库更改过的代码或信息同步到线上的github的仓库里
2.GitHub
是可以在线上创建自己的远程仓库
可以在线上进行访问，浏览的网站
各个工作人员的代码或信息放到GitHub进行托管的网站

3.使用git向github push的流程
在打开git的工作界面时，会分为4个工作区域，在git的右上区域为工作区，该区域会对该仓库进行查找变动和stage的操作
在工作区进行之后，将已改动的文件放入到暂存区（左下角），之后在右下角的区域中对该文件添加备注，之后进行commit操作
在此之后，又将修改过的文件放入到本地仓库当中，之后进行push的操作，将修改过的文件传到远程仓库中。

4.使用git将远程仓库中文件与本地文件同步的流程
使用git时，会将远程仓库中的文档变动进行查找变动（remote),之后会将改动后的文件放到本地仓库中
再通过使用merge,将改动后的文件与本地文件合并（在工作区中进行）
